import { APIService } from "./api.service";

class StandardValue extends APIService<StandardValue> {}

// eslint-disable-next-line import/no-anonymous-default-export
export default new StandardValue();
